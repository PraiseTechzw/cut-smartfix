PORT=4000
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
STORAGE_BUCKET=maintenance-evidence
# Add your LAN IP (run `ip addr` / `ipconfig`) so the mobile app can reach the API.
# Also add the Expo dev server origin (exp://<LAN-IP>:8081) if using web previews.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
