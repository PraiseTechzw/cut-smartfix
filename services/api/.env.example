PORT=4000
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
STORAGE_BUCKET=maintenance-evidence
# Native mobile requests do not need CORS. Use allow-all for local devices/web previews.
CORS_ALLOW_ALL=true
# For production, set CORS_ALLOW_ALL=false and list exact HTTPS web origins below.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
